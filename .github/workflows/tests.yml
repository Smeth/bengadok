# Ce workflow est remplacé par ci.yml
# Conservé pour compatibilité avec les anciennes références
name: tests

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Voir ci.yml
        run: echo "Utiliser le workflow ci.yml à la place"
